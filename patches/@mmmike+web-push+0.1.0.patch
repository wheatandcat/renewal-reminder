diff --git a/node_modules/@mmmike/web-push/dist/send.mjs b/node_modules/@mmmike/web-push/dist/send.mjs
index 06a1740..2759018 100644
--- a/node_modules/@mmmike/web-push/dist/send.mjs
+++ b/node_modules/@mmmike/web-push/dist/send.mjs
@@ -1,7 +1,7 @@
 import { createVapidJwt, urlBase64ToUint8Array } from './vapid.mjs';
 
 const sendPushNotification = async (subscription, payload, vapid, options = {}) => {
-  const { logger, ttl = 86400 } = options;
+  const { logger, ttl = 86400, urgency } = options;
   const url = new URL(subscription.endpoint);
   const audience = `${url.protocol}//${url.host}`;
   const jwt = await createVapidJwt({
@@ -23,7 +23,8 @@ const sendPushNotification = async (subscription, payload, vapid, options = {})
       Authorization: `vapid t=${jwt}, k=${vapid.publicKey}`,
       "Content-Encoding": "aes128gcm",
       "Content-Type": "application/octet-stream",
-      TTL: String(ttl)
+      TTL: String(ttl),
+      ...(urgency ? { Urgency: urgency } : {})
     },
     body: encryptedPayload
   });
diff --git a/node_modules/@mmmike/web-push/dist/shared/web-push.CQqM10dy.d.mts b/node_modules/@mmmike/web-push/dist/shared/web-push.CQqM10dy.d.mts
index 74d8da8..0acfe5a 100644
--- a/node_modules/@mmmike/web-push/dist/shared/web-push.CQqM10dy.d.mts
+++ b/node_modules/@mmmike/web-push/dist/shared/web-push.CQqM10dy.d.mts
@@ -56,6 +56,8 @@ type SendPushOptions = {
     logger?: Logger;
     /** TTL in seconds (default: 86400 = 24 hours) */
     ttl?: number;
+    /** Urgency header per RFC 8030 (default: "normal"). Use "high" so Android shows a heads-up notification. */
+    urgency?: "very-low" | "low" | "normal" | "high";
 };
 
 export type { Logger as L, PushPayload as P, SendPushOptions as S, VapidConfig as V, PushSubscriptionData as a };
diff --git a/node_modules/@mmmike/web-push/dist/shared/web-push.CQqM10dy.d.ts b/node_modules/@mmmike/web-push/dist/shared/web-push.CQqM10dy.d.ts
index 74d8da8..0acfe5a 100644
--- a/node_modules/@mmmike/web-push/dist/shared/web-push.CQqM10dy.d.ts
+++ b/node_modules/@mmmike/web-push/dist/shared/web-push.CQqM10dy.d.ts
@@ -56,6 +56,8 @@ type SendPushOptions = {
     logger?: Logger;
     /** TTL in seconds (default: 86400 = 24 hours) */
     ttl?: number;
+    /** Urgency header per RFC 8030 (default: "normal"). Use "high" so Android shows a heads-up notification. */
+    urgency?: "very-low" | "low" | "normal" | "high";
 };
 
 export type { Logger as L, PushPayload as P, SendPushOptions as S, VapidConfig as V, PushSubscriptionData as a };
